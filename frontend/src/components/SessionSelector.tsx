/**
 * Session selector component for choosing which race to view
 */

import React from "react";

export const SessionSelector: React.FC = () => {
  return <div>Session Selector</div>;
};

export default SessionSelector;
